# Enable if: QUICK_TEST

set Dimension                    = 2
set End time                     = 5
set Maximum time step            = 1
set Nonlinear solver scheme     = single Advection, iterated Stokes
set Output directory             = output-viscoelastic-displacement-only-radial
set Pressure normalization       = no
set Use operator splitting       = true
set Use years instead of seconds = false

subsection Solver parameters
  subsection Operator splitting parameters
    set Reaction solver type = fixed step
  end
  subsection Stokes solver parameters
    set Stokes solver type = block AMG
    set Linear solver tolerance = 1e-12
  end
end

subsection Geometry model
  set Model name = spherical shell
  subsection Spherical shell
    set Inner radius  = 1
    set Outer radius  = 2
    set Opening angle = 90
  end
end

subsection Mesh refinement
  set Initial adaptive refinement = 0
  set Initial global refinement   = 0
end

subsection Discretization
  set Use discontinuous composition discretization = true
end

subsection Formulation
  set Enable elasticity = true
  set Mass conservation = elastic pressure evolution
  subsection Density sources
    set Reference density model     = constant
    set Density source law          = mechanical mass conservation
    set Constant reference density  = 4000
  end
end

subsection Boundary velocity model
  set Tangential velocity boundary indicators =
  set Prescribed velocity boundary indicators = 0: function, 1: function, 2: function, 3: function
  set Zero velocity boundary indicators       =
  subsection Function
    set Variable names      = x,y
    set Function expression = 0.01*x; 0.01*y
  end
end

subsection Compositional fields
  set Number of fields = 7
  set Compositional field methods = field, field, field, field, field, field, static
  set Names of fields  = ve_stress_xx, ve_stress_yy, ve_stress_xy, ve_stress_xx_old, ve_stress_yy_old, ve_stress_xy_old, ve_radial_displacement
  set Types of fields  = stress, stress, stress, stress, stress, stress, generic
end

subsection Initial composition model
  set Model name = function
  subsection Function
    set Function expression = 0; 0; 0; 0; 0; 0; 0
  end
end

subsection Boundary composition model
  set Fixed composition boundary indicators =
  set List of model names                    = initial composition
end

subsection Initial temperature model
  set Model name = function
  subsection Function
    set Function expression = 0
  end
end

subsection Material model
  set Model name = viscoelastic
  subsection Viscoelastic
    set Densities                         = 4000
    set Viscosities                       = 1000
    set Elastic shear moduli             = 300
    set Elastic bulk modulus formulation = bulk modulus
    set Elastic bulk moduli               = 2000
    set Enable compressible Maxwell       = true
    set Initial response mode             = instantaneous elastic
    set Use fixed elastic time step       = true
    set Initial elastic time step         = 1
  end
end

subsection Gravity model
  set Model name = radial constant
  subsection Radial constant
    set Magnitude = 0
  end
end

subsection Postprocess
  set List of postprocessors = viscoelastic displacement-only radial
end
