# Exercise the assembled internal density-jump restoring term with a small
# nonzero radial-displacement history. This is the block-AMG diagnostic for the
# corresponding local-smoothing block-GMG test.
# Enable if: QUICK_TEST
# DEPENDS-ON: density_source_manager_tabulated_radial_piecewise

include $ASPECT_SOURCE_DIR/tests/density_source_manager_tabulated_radial_piecewise.prm

set End time                   = 0
set Nonlinear solver tolerance = 1e-10

subsection Gravity model
  subsection Radial constant
    set Magnitude = 10
  end
end

subsection Initial composition model
  subsection Function
    set Function expression = 0; 0; 0; 0; 0; 0; 0.01
  end
end

subsection Mesh refinement
  set Initial adaptive refinement = 0
  set Initial global refinement   = 1
end

subsection Material model
  set Material averaging = harmonic average only viscosity
end

subsection Solver parameters
  subsection Stokes solver parameters
    set Linear solver tolerance = 1e-12
    set Stokes solver type      = block AMG
  end
end

subsection Postprocess
  set List of postprocessors = density source manager tabulated radial, point values, \
                               pressure statistics, velocity statistics

  subsection Point values
    set Evaluation points = 1.25, 0; 1.625, 0; 1.875, 0; \
                            0, 1.25; 0, 1.625; 0, 1.875
  end
end
